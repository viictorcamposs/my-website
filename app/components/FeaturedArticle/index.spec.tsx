// import { screen, render } from '@testing-library/custom'

// import FeaturedArticle from './index'

// async function renderFeaturedArticle() {
//   render(<FeaturedArticle article={{}} />)
// }

describe('FeaturedArticle', () => {
  // beforeEach(async () => await renderFeaturedArticle())

  it('should render proper content', async () => {
    // renderMostRecentArticle()
    // expect(screen.getByText(featuredArticle.data.title[0].text)).toBeInTheDocument()
    // expect(screen.getByText(featuredArticle.data.description[0].text)).toBeInTheDocument()
    // expect(screen.getByRole('link').getAttribute('href')).toContain(featuredArticle.url)
    // expect(screen.getByRole('img')).toHaveProperty('alt', featuredArticle.data.image.alt)
    // expect(screen.getByRole('img').getAttribute('src')).toContain(
    //   encodeURIComponent(featuredArticle.data.image.url)
    // )
  })
})
